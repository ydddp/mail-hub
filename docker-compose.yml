services:
  mail-hub:
    # 默认拉取 GHCR 镜像；本地构建请注释 image 并取消 build 的注释
    image: ghcr.io/ydddp/mail-hub:latest
    # build: .
    ports:
      - "3100:3100"
    volumes:
      - ./data:/app/data
    environment:
      - API_SECRET=${API_SECRET}
    restart: unless-stopped
